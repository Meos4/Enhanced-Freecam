#pragma once

#include "Types.hpp"

namespace Software
{
	inline constexpr auto name{ "Enhanced Freecam" }, version{ "1.2.1" };

	inline constexpr auto
		width{ static_cast<s32>(1600.f / 1.5f) },
		height{ static_cast<s32>(900.f / 1.5f) };
}